# Alembic configuration for the Locations schema only.
#
# Run migrations with:
#   uv run alembic -c alembic-locations.ini upgrade head
#
# The URL comes from LOCATIONS_DATABASE_URL at run time (see migrations/locations/env.py).

[alembic]
script_location = migrations/locations
prepend_sys_path = .
path_separator = os

sqlalchemy.url =

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
