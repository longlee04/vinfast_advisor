# Nghiệm thu A8-4 — Thông báo nội bộ

File test: `tests/agents/integration/test_notices.py`, `tests/agents/unit/api/test_notice_authorization.py`
File code mới: `src/agents/services/operations/notices.py`, `src/agents/api/__init__.py`, `src/agents/api/security.py`, `src/agents/api/schemas.py`, `src/agents/api/notice_routes.py`
File code sửa: `src/agents/adapters/repositories.py` (thay chỗ giữ chỗ `notices` bằng repository thật)

## 1. Đối chiếu Test bắt buộc → bằng chứng

| Điều kiện (nguyên văn từ `phan-cong-thanh-vien.md`) | Tên hàm test | Kết quả |
|---|---|---|
| Hai tư vấn viên đọc cùng notice → **hai hàng `notice_reads` độc lập** | `test_two_advisors_reading_one_notice_create_independent_rows` | PASSED |
| `CUSTOMER` gọi API catalog Admin → 403 | `test_customer_calling_the_admin_notice_api_is_forbidden` | PASSED |
| `ADVISOR` gọi API catalog Admin → 403 | `test_advisor_calling_the_admin_notice_api_is_forbidden` | PASSED |

Dòng test bắt buộc thứ hai (`CUSTOMER`/`ADVISOR` → 403) được tách thành **hai hàm test riêng** theo DoD điều 1 — hai vai trò là hai điều kiện, gộp một hàm thì đỏ một cái là mù cả hai.

Test âm và test biên kèm theo (DoD điều 2):

| Nhánh code | Tên hàm test | Kết quả |
|---|---|---|
| Cùng một người đánh dấu hai lần → vẫn đúng một hàng | `test_same_advisor_reading_twice_keeps_one_row` | PASSED |
| Đánh dấu notice không tồn tại → `NoticeNotFoundError`, không ghi hàng nào | `test_marking_an_unknown_notice_is_rejected` | PASSED |
| Danh sách trả đúng cờ đã đọc theo từng người | `test_listing_reports_read_state_per_advisor` | PASSED |
| `ADMIN` đăng được (chứng minh 403 không phải do endpoint hỏng) | `test_admin_can_publish_a_notice` | PASSED |
| `ADVISOR` đánh dấu đã đọc được (guard chặn đăng, không chặn đọc) | `test_advisor_can_mark_a_notice_as_read` | PASSED |
| `CUSTOMER` không đánh dấu đã đọc được | `test_customer_cannot_mark_a_notice_as_read` | PASSED |

Chi tiết assert của test bắt buộc thứ nhất: sau khi `advisor-a` và `advisor-b` cùng đánh dấu một notice → **đếm** số hàng trong `notice_reads` phải bằng **2**, và đọc ra đúng hai `advisor_id` khác nhau. Đếm số hàng, không chỉ đọc giá trị.

## 2. Bẫy dễ sai đã xử lý thế nào

Cột "Bẫy dễ sai" của A8-4 trong `phan-cong-thanh-vien.md` là `—` (không có bẫy nào được ghi).

Hai rủi ro thật của task này đã được kiểm chứng bằng đột biến:

1. **Khoá chống trùng đặt sai** — tạm đổi `on_conflict_do_nothing(index_elements=["notice_id", "advisor_id"])` thành chỉ `["notice_id"]` (tức coi "đã đọc" là trạng thái chung của notice, không phải của từng người). Kết quả: **3 test đỏ**, gồm cả test bắt buộc "hai hàng độc lập". Khôi phục → xanh.
2. **Guard sai vai trò** — tạm đổi `Depends(require_admin)` thành `Depends(require_staff)` ở endpoint đăng notice. Kết quả: `test_advisor_calling_the_admin_notice_api_is_forbidden` **đỏ** (đúng cái phải đỏ), còn test `CUSTOMER` vẫn xanh — chứng minh hai hàm test tách riêng thật sự soi được hai vai trò khác nhau. Khôi phục → xanh.

## 3. Kết quả 3 lệnh kiểm tra

```
$ uv run pytest tests/agents -q
176 passed, 1 warning in 92.40s (0:01:32)

$ uv run ruff check src/agents tests/agents
All checks passed!

$ uv run mypy src/agents
src/agents/adapters/feature_retriever.py:131: error: Argument "status" to "FeatureAssertion" has incompatible type "str"; expected "Literal['YES', 'NO', 'UNKNOWN']"  [arg-type]
src/agents/adapters/feature_retriever.py:282: error: Incompatible types in assignment (expression has type "float", variable has type "SQLCoreOperations[Decimal | None] | Decimal | None")  [assignment]
Found 2 errors in 1 file (checked 56 source files)
```

Ghi chú về mypy: vẫn đúng 2 lỗi tồn đọng của Khối 1 (Dương), đã ghi từ nghiệm thu A7-1. Code mới của A8-4 không sinh thêm lỗi nào (56 file được kiểm, tăng 5 file so với A7-2).

Cảnh báo duy nhất trong pytest là `StarletteDeprecationWarning` của `fastapi.testclient` — thư viện, không phải code của task.

## 4. Toàn bộ suite

Đã chạy `uv run pytest -q` (toàn repo):

| Mốc | Kết quả |
|---|---|
| Sau A7-2 | `6 failed, 919 passed, 4 skipped, 102 errors` |
| Sau A8-4 | `6 failed, 928 passed, 4 skipped, 102 errors` |

- Chênh lệch đúng **+9 passed** = 9 test của A8-4. `failed` và `errors` **không đổi**.
- Test biên A0-3 "`nodes/` không import `services/operations/`" vẫn xanh: A8-4 không đụng `nodes/`.
- Contract dùng chung: **không sửa** `ports.py`, `contracts.py`, `state.py`, `domain/values.py`, `services/registry.py`, `composition.py`, `nodes/__init__.py`. Service operations **không** được thêm vào `AgentServices` (đúng ranh giới A0-3: registry chỉ chứa nhóm advisory).

## 5. Điểm lệch so với plan

Mục "Cần chốt trước khi code" của `A8-4.md` có 4 điểm, đã hỏi và được chốt trước khi viết code:

| # | Điểm | Quyết định cuối | Lý do |
|---|---|---|---|
| 1 | "API catalog Admin" không thuộc file sở hữu Khối 4; A10-5 của Dương lại chờ A8-4 | A8-4 dựng **guard phân quyền dùng chung** (`src/agents/api/security.py`) + endpoint notice của Admin, và test 403 trên chính endpoint đó | Đúng nghĩa "A10-5 chờ A8-4": Dương gắn `require_admin` vào endpoint catalog của mình thay vì viết cơ chế phân quyền thứ hai. Không lấn sang phạm vi Khối 1 |
| 2 | Ai được đăng notice | Chỉ `ADMIN`; `CUSTOMER` và `ADVISOR` đều 403 | Khớp mô tả gốc A8-4 ở `vinfast-agent-mvp.md`: "Admin đăng; tư vấn viên thấy" |
| 3 | `notice_reads` sinh lúc nào | Hành động tường minh: `POST /agent/notices/{id}/read` | `GET` không ghi DB; gọi lại lần hai không sinh hàng thứ hai nhờ khoá chính ghép |
| 4 | Có test luồng "phiên đăng nhập kế tiếp" không | Không thêm | Cột "Test bắt buộc" không có dòng nào về việc này; DoD điều 1 chỉ đòi đúng các dòng đã ghi |

Ba điều phát sinh trong lúc code, quyết định như sau:

1. **Thêm file `src/agents/api/security.py`** — ngoài danh sách file sở hữu ghi trong `phan-cong-thanh-vien.md` (danh sách đó chỉ liệt kê `api/*_routes.py` và `api/schemas.py`). Lý do: guard phải dùng chung được cho A10-5 của Dương theo quyết định điểm 1; nhét nó vào `notice_routes.py` thì Dương phải import từ file notice — sai chỗ. File này không trùng quyền sở hữu của ai (`api/routes.py` và `api/dependencies.py` là của Đạt).
2. **`current_staff` hiện trả 401 và chờ được nối.** Danh tính thật nằm trong phiên đăng nhập của `src/auth/`, mà app chính chưa nối router agent (việc nối `main.py` thuộc A4-4 của Đạt). Đã làm điểm nối tường minh: chưa nối thì **chặn**, không mặc định cho qua. Test bơm danh tính bằng `dependency_overrides` theo đúng quyết định "app FastAPI nhỏ trong test".
3. **Thêm `exists` và `list_for` cho `SqlAlchemyNoticeRepository`** — ngoài `ports.NoticeRepository` (không đổi port). Lý do: `mark_read` một `notice_id` không tồn tại phải báo lỗi rõ thay vì để khoá ngoại nổ ra lỗi DB thô; và endpoint danh sách cần cờ đã đọc theo từng người. Service nhìn hai method này qua Protocol hẹp `NoticeStore` khai báo trong `services/operations/notices.py`, nên `services/` vẫn không import SQLAlchemy.

Một điểm tồn đọng mang sang: `uv run mypy src/agents` vẫn còn 2 lỗi của Khối 1, đã chốt hướng "báo Dương, Khối 4 code tiếp".

**Việc A8-4 mở khoá cho người khác:** Dương có thể bắt đầu A10-5 — import `require_admin` từ `src/agents/api/security.py` và gắn vào endpoint duyệt hàng loạt của Khối 1.
