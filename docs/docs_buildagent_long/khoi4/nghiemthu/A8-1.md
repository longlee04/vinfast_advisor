# Nghiệm thu A8-1 — Migration `agent_0005`

File code: `tests/agents/integration/test_agent_0005_migration.py`
Migration được nghiệm thu: `migrations/agents/versions/agent_0005_booking_notice.py` (không sửa dòng nào)

## 1. Đối chiếu Test bắt buộc → bằng chứng

| Điều kiện (nguyên văn từ `phan-cong-thanh-vien.md`) | Tên hàm test | Kết quả |
|---|---|---|
| `upgrade` sạch (nửa đầu của "`upgrade`/`downgrade` sạch") | `test_agent_0005_upgrade_creates_booking_and_notice_tables` | PASSED |
| `downgrade` sạch (nửa sau của "`upgrade`/`downgrade` sạch") | `test_agent_0005_downgrade_removes_only_booking_and_notice_tables` | PASSED |

Nội dung assert của từng hàm (mức "sạch" áp y như A7-1 — xem mục 5):

- `test_agent_0005_upgrade_creates_booking_and_notice_tables`: alembic exit code 0 → `agent_alembic_version` = `agent_0005` → cả 3 bảng `test_drive_bookings`, `internal_notices`, `notice_reads` có mặt.
- `test_agent_0005_downgrade_removes_only_booking_and_notice_tables`: alembic exit code 0 → revision quay về `agent_0004` → cả 3 bảng trên **không còn** → bảng `review_queue` của `agent_0004` **vẫn còn nguyên**.

## 2. Bẫy dễ sai đã xử lý thế nào

Cột "Bẫy dễ sai" của A8-1 trong `phan-cong-thanh-vien.md` là `—` (không có bẫy nào được ghi).

Rủi ro xanh giả của loại task này đã xử lý bằng kiểm chứng đột biến, giống A7-1:

- Tạm xoá `op.drop_table("internal_notices")` khỏi `downgrade()` của `agent_0005` → `test_agent_0005_downgrade_removes_only_booking_and_notice_tables` **FAILED**.
- Khôi phục bằng `git checkout --` → chạy lại → 2 passed.
- Kết luận: test bắt được trường hợp downgrade sót **một trong ba** bảng, không chỉ bắt được lỗi tổng.

## 3. Kết quả 3 lệnh kiểm tra

```
$ uv run pytest tests/agents -q
161 passed in 76.45s (0:01:16)

$ uv run ruff check src/agents tests/agents
All checks passed!

$ uv run mypy src/agents
src/agents/adapters/feature_retriever.py:131: error: Argument "status" to "FeatureAssertion" has incompatible type "str"; expected "Literal['YES', 'NO', 'UNKNOWN']"  [arg-type]
src/agents/adapters/feature_retriever.py:282: error: Incompatible types in assignment (expression has type "float", variable has type "SQLCoreOperations[Decimal | None] | Decimal | None")  [assignment]
Found 2 errors in 1 file (checked 48 source files)
```

Ghi chú về mypy: đúng 2 lỗi đã ghi ở nghiệm thu A7-1, thuộc `src/agents/adapters/feature_retriever.py` của Khối 1 (Dương). A8-1 không thêm/sửa dòng nào trong `src/`. Đã chốt hướng xử lý: báo Dương, Khối 4 code tiếp (mục 5).

## 4. Toàn bộ suite

Đã chạy `uv run pytest -q` (toàn repo):

| Mốc | Kết quả |
|---|---|
| Sau A7-1 | `6 failed, 911 passed, 4 skipped, 102 errors` |
| Sau A8-1 | `6 failed, 913 passed, 4 skipped, 102 errors` |

- Chênh lệch đúng **+2 passed** = 2 test của A8-1. `failed` và `errors` **không đổi**.
- 6 test đỏ và 102 errors là nợ có sẵn của repo ở `tests/locations`, `tests/test_codex_hooks.py`, `tests/test_product` — không cái nào thuộc `tests/agents`.
- Không đụng contract dùng chung: A8-1 không sửa `ports.py`, `contracts.py`, `state.py`, `domain/values.py`, `services/registry.py`, `composition.py`, `nodes/__init__.py`. Không sửa `agent_0004` mà A7-1 đã nghiệm thu.

## 5. Điểm lệch so với plan

Mục "Cần chốt trước khi code" của `A8-1.md` có 3 điểm, đã hỏi và được chốt trước khi viết code:

| # | Điểm | Quyết định cuối | Lý do |
|---|---|---|---|
| 1 | Định nghĩa "sạch" | Áp y như A7-1 | Ba file test migration (`agent_0003`, `agent_0004`, `agent_0005`) cùng một khuôn, người nghiệm thu đọc một lần hiểu cả ba |
| 2 | 3 bảng → tách test thế nào | 2 hàm test, mỗi hàm assert gộp cả 3 bảng | Cột "Test bắt buộc" chỉ có 2 ý (upgrade, downgrade) nên 2 hàm là đúng DoD điều 1; kiểm chứng đột biến ở mục 2 đã chứng minh cách gộp này vẫn bắt được lỗi ở từng bảng riêng lẻ |
| 3 | Test âm | Hàm downgrade đóng vai trò test âm | Thống nhất với quyết định đã chốt ở A7-1 |

Điểm phát sinh mang sang từ A7-1, đã có quyết định:

- **`uv run mypy src/agents` chưa xanh** vì 2 lỗi trong file của Khối 1. Đã chốt: báo Dương, Khối 4 tiếp tục code, mỗi file nghiệm thu ghi rõ đây là nợ ngoài khối kèm bằng chứng không phải do task của Khối 4 gây ra.
