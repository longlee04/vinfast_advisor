# Nghiệm thu A7-1 — Migration `agent_0004`

File code: `tests/agents/integration/test_agent_0004_migration.py`
Migration được nghiệm thu: `migrations/agents/versions/agent_0004_review_queue.py` (không sửa dòng nào)

## 1. Đối chiếu Test bắt buộc → bằng chứng

| Điều kiện (nguyên văn từ `phan-cong-thanh-vien.md`) | Tên hàm test | Kết quả |
|---|---|---|
| `upgrade` sạch (nửa đầu của "`upgrade`/`downgrade` sạch") | `test_agent_0004_upgrade_creates_review_queue_table` | PASSED |
| `downgrade` sạch (nửa sau của "`upgrade`/`downgrade` sạch") | `test_agent_0004_downgrade_removes_only_review_queue_table` | PASSED |

Nội dung assert của từng hàm (theo mức "sạch" đã chốt — xem mục 5):

- `test_agent_0004_upgrade_creates_review_queue_table`: alembic exit code 0 → `agent_alembic_version` = `agent_0004` → bảng `review_queue` có mặt trong danh sách bảng.
- `test_agent_0004_downgrade_removes_only_review_queue_table`: alembic exit code 0 → `agent_alembic_version` quay về `agent_0003` → `review_queue` **không còn** trong danh sách bảng → 6 bảng của `agent_0003` (`agent_runs`, `run_snapshots`, `run_candidates`, `run_evidence`, `scoring_result`, `tco_estimates`) **vẫn còn nguyên**.

## 2. Bẫy dễ sai đã xử lý thế nào

Cột "Bẫy dễ sai" của A7-1 trong `phan-cong-thanh-vien.md` là `—` (không có bẫy nào được ghi).

Tuy vậy có một rủi ro thật của loại task này — migration đã tồn tại sẵn nên test viết ra có thể xanh giả mà không kiểm được gì. Đã xử lý bằng một lần kiểm chứng đột biến:

- Tạm xoá `op.drop_table("review_queue")` khỏi `downgrade()` của `agent_0004` → chạy lại test → `test_agent_0004_downgrade_removes_only_review_queue_table` **FAILED** đúng như mong đợi.
- Khôi phục file bằng `git checkout --` → chạy lại → 2 passed.
- Kết luận: test thật sự bắt được migration downgrade sót bảng, không phải xanh giả.

## 3. Kết quả 3 lệnh kiểm tra

```
$ uv run pytest tests/agents -q
159 passed in 62.93s (0:01:02)

$ uv run ruff check src/agents tests/agents
All checks passed!

$ uv run mypy src/agents
src/agents/adapters/feature_retriever.py:131: error: Argument "status" to "FeatureAssertion" has incompatible type "str"; expected "Literal['YES', 'NO', 'UNKNOWN']"  [arg-type]
src/agents/adapters/feature_retriever.py:282: error: Incompatible types in assignment (expression has type "float", variable has type "SQLCoreOperations[Decimal | None] | Decimal | None")  [assignment]
Found 2 errors in 1 file (checked 48 source files)
```

Ghi chú về mypy: 2 lỗi **không thuộc A7-1 và không thuộc Khối 4**. Chúng nằm ở `src/agents/adapters/feature_retriever.py` — file thuộc Khối 1 (Dương). A7-1 không thêm/sửa một dòng nào trong `src/`, chỉ thêm một file test mới. Xem mục 5.

## 4. Toàn bộ suite

Đã chạy `uv run pytest -q` (toàn repo), đo hai lần để tách phần của mình khỏi nợ có sẵn:

| Lần chạy | Kết quả |
|---|---|
| Baseline — tạm gỡ file test của A7-1 | `6 failed, 909 passed, 4 skipped, 102 errors` |
| Sau khi có file test của A7-1 | `6 failed, 911 passed, 4 skipped, 102 errors` |

- Chênh lệch đúng **+2 passed** = 2 test của A7-1. Số `failed` và `errors` **không đổi**.
- 6 test đỏ có sẵn: `tests/locations/integration/test_http.py` (4), `tests/test_codex_hooks.py` (1), `tests/test_product/test_tco_repository.py` (1). 102 errors đều ở `tests/locations/integration/test_repositories.py`. Không cái nào thuộc `tests/agents`.
- Không đụng contract dùng chung: A7-1 không sửa `ports.py`, `contracts.py`, `state.py`, `domain/values.py`, `services/registry.py`, `composition.py`, `nodes/__init__.py`.

## 5. Điểm lệch so với plan

Mục "Cần chốt trước khi code" của `A7-1.md` có 3 điểm, đã hỏi và được chốt trước khi viết code:

| # | Điểm | Quyết định cuối | Lý do |
|---|---|---|---|
| 1 | "sạch" nghĩa là gì | Theo tiền lệ A5-1: exit code 0 + revision đúng + upgrade tạo đủ bảng + downgrade xoá đúng bảng của mình và không đụng bảng migration trước | Giữ đồng nhất với `test_agent_0003_migration.py` đang xanh trong repo, người nghiệm thu đọc hai file thấy cùng một khuôn |
| 2 | Có được sửa `agent_0004` không | Được sửa thẳng nếu test đỏ | File thuộc quyền sở hữu Khối 4. **Thực tế không phải sửa** — migration hiện tại đã thoả cả 2 test |
| 3 | Test âm cho task migration | Chính hàm test downgrade đóng vai trò test âm (sau downgrade, `review_queue` phải **không** còn) | Đúng cách A5-1 đã được nghiệm thu; không thêm hàm test thứ ba |

Một điểm phát sinh ngoài dự kiến, cần anh quyết trước khi sang task sau:

- **`uv run mypy src/agents` chưa xanh** vì 2 lỗi trong `src/agents/adapters/feature_retriever.py` (Khối 1 — Dương). Đã xác minh 2 lỗi này có sẵn từ trước A7-1 (A7-1 không sửa `src/`). DoD điều 5 đòi cả 3 lệnh xanh, nên A7-1 **đạt phần của mình nhưng chưa đạt chữ của DoD điều 5** cho tới khi 2 lỗi kia được xử lý. Chưa tự sửa vì file thuộc khối khác.
