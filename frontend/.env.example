NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# Bật (mặc định) = /advisor, /admin không chặn theo role (backend chưa nối).
# false = chặn thật theo role từ /me, chưa đăng nhập redirect /login.
NEXT_PUBLIC_DEMO_MODE=false
