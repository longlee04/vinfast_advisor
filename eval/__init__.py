"""[A9-3] Công cụ nghiệm thu offline — biên một chiều: `eval/` biết `src/`, `src/` không biết `eval/`.

Mọi thứ ở đây là công cụ kiểm thử (cùng loại `pytest`), **không** nằm trên đường
chạy thật và không đụng p95.
"""
